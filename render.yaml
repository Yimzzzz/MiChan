services:
  - type: web
    name: michan
    env: node
    region: oregon
    plan: free
    buildCommand: bash build.sh
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
